apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

patches:
  - target:
      kind: Deployment
      name: nginx
    path: deployment-patch.yaml
  - target:
      kind: Service
      name: nginx
    path: service-patch.yaml
      
